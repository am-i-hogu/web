import { Meta, Subtitle, Title } from "@storybook/addon-docs/blocks";

<Meta title="Docs/Overview" />

<Title>Storybook Docs</Title>
<Subtitle>Storybook UI 관련 공지사항</Subtitle>

## 공지사항

- 현재 Storybook은 공용 UI 기준으로 정리되어 있으며, 신규 컴포넌트는 동일한 작성 양식을 따라 추가해주세요.
- 작성 기준은 `Docs/Storybook Guide` 문서를 참고해주세요.
- 컴포넌트 API 변경 시, 구현 코드와 Storybook 문서를 같은 PR에서 함께 업데이트하는 것을 원칙으로 합니다.

---
<br />
### 4/26
- 공용 컴포넌트 1차 작업 완료
- 추가된 컴포넌트: `Button`, `Input`, `Checkbox`, `RadioOption`, `Modal`, `BottomSheet`, `EmptyState`, `Toast`, `WithdrawFlow`

---
<br/>
### 4/30 - 5/1
- 공용 컴포넌트 2차 작업 완료
- 추가된 컴포넌트: `ActionButtons`, `Textfield(Comment/Search/Default)`, `SortSelect`, `TopScrollButton`, `FilterSortBar`

- 공용 컴포넌트 리팩토링 및 문서화 보완
- 추가/갱신된 컴포넌트: `Textarea`, `Input/Textfield` 계열 스토리 정리
- Storybook 탐색성 개선: `UI/<그룹>/<컴포넌트명>` 타이틀 그룹화(`Button`, `Input`)
- 카테고리/정렬 UI는 `FilterSortBar` 공용 컴포넌트 기준으로 정리
